type Message @entity {
  id: ID!
  data: Bytes!
  recipient: String! # address
  attester: String! # address
  time: BigInt!
  revoked: Boolean!
  user: User
}

type User @entity {
  id: ID!
  usernameData: Bytes!
  updated: BigInt!
}
